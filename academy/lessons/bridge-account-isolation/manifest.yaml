id: bridge.account-isolation
track: bridge-engineer
title: Account Isolation
updated_at: '2026-07-19'
estimated_minutes: 50
objectives:
- Explain how to prevent cross-account routing and state leakage.
- Produce a deterministic trace and fault-evidence artifact.
prerequisites:
- bridge.heartbeat
tags:
- executable
- paper-only
- duplicate
translations:
  en:
    title: Account Isolation
    summary: Implement and prove how to prevent cross-account routing and state leakage.
  es:
    title: Aislamiento de cuentas
    summary: Implementa y demuestra cómo evitar rutas y fugas entre cuentas.
hints:
- Read the visible assertion, then identify how to prevent cross-account routing and state leakage; inspect
  `isolate_account_namespace` as a naming clue.
- The target behavior is represented by one explicit operation near `isolate_account_namespace`.
- Replay the duplicate profile and inspect its required recovery operation.
execution:
  runner: nexural.declarative-trace.v1
  starter: starter/program.yaml
  visible_tests: tests/visible.yaml
  hidden_tests: tests/hidden.yaml
  solution: solution/program.yaml
  expected_trace: expected-trace.json
  fault_profiles:
  - duplicate
rubric:
- id: trace-complete
  metric: tests.trace-complete.passed
  operator: equals
  expected: true
  weight: 25
  visibility: public
  message: Replace every TODO so the trusted runner can complete the trace.
- id: target-behavior
  metric: tests.target-behavior.passed
  operator: equals
  expected: true
  weight: 25
  visibility: public
  message: The trace must implement `isolate_account_namespace`.
- id: paper-boundary
  metric: tests.paper-boundary.passed
  operator: equals
  expected: true
  weight: 25
  visibility: hidden
  message: The hidden safety boundary requires paper-only mode.
- id: fault-resilience
  metric: fault_evidence.duplicate.handled
  operator: equals
  expected: true
  weight: 25
  visibility: hidden
  message: The implementation did not safely handle the seeded duplicate scenario.
