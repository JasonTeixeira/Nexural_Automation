id: strategy.managed-orders
track: strategy-builder
title: Managed Order Semantics
updated_at: '2026-07-19'
estimated_minutes: 50
objectives:
- Explain how to model entry handling and signal-name constraints.
- Produce a deterministic trace and fault-evidence artifact.
prerequisites:
- strategy.state-machine
tags:
- executable
- paper-only
- duplicate
translations:
  en:
    title: Managed Order Semantics
    summary: Implement and prove how to model entry handling and signal-name constraints.
  es:
    title: Semántica de órdenes administradas
    summary: Implementa y demuestra cómo modelar reglas de entrada y nombres de señal.
hints:
- Read the visible assertion, then identify how to model entry handling and signal-name constraints; inspect
  `model_managed_order_rules` as a naming clue.
- The target behavior is represented by one explicit operation near `model_managed_order_rules`.
- Replay the duplicate profile and inspect its required recovery operation.
execution:
  runner: nexural.declarative-trace.v1
  starter: starter/program.yaml
  visible_tests: tests/visible.yaml
  hidden_tests: tests/hidden.yaml
  solution: solution/program.yaml
  expected_trace: expected-trace.json
  fault_profiles:
  - duplicate
rubric:
- id: trace-complete
  metric: tests.trace-complete.passed
  operator: equals
  expected: true
  weight: 25
  visibility: public
  message: Replace every TODO so the trusted runner can complete the trace.
- id: target-behavior
  metric: tests.target-behavior.passed
  operator: equals
  expected: true
  weight: 25
  visibility: public
  message: The trace must implement `model_managed_order_rules`.
- id: paper-boundary
  metric: tests.paper-boundary.passed
  operator: equals
  expected: true
  weight: 25
  visibility: hidden
  message: The hidden safety boundary requires paper-only mode.
- id: fault-resilience
  metric: fault_evidence.duplicate.handled
  operator: equals
  expected: true
  weight: 25
  visibility: hidden
  message: The implementation did not safely handle the seeded duplicate scenario.
