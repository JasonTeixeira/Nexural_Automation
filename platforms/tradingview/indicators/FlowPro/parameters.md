# Flow Pro — Parameters

| Input | Type | Default | Range | Notes |
| ----- | ---- | ------- | ----- | ----- |
| _example_ | int | 14 | 5–50 | _Replace with real inputs from src/FlowPro.pine_ |

Fill this table out from the actual inputs in `src/FlowPro.pine`. Document
every parameter that a user can change, even if you think the default is the
only sensible value.
