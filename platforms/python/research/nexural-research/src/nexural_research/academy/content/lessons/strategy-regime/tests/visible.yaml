tests:
- id: trace-complete
  path: trace.status
  operator: equals
  expected: complete
  message: Replace every TODO so the trusted runner can complete the trace.
- id: target-behavior
  path: source.program.operations
  operator: contains
  expected: apply_regime_gate
  message: The trace must implement `apply_regime_gate`.
