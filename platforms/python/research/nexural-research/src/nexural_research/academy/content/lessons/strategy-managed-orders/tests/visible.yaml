tests:
- id: trace-complete
  path: trace.status
  operator: equals
  expected: complete
  message: Replace every TODO so the trusted runner can complete the trace.
- id: target-behavior
  path: source.program.operations
  operator: contains
  expected: model_managed_order_rules
  message: The trace must implement `model_managed_order_rules`.
