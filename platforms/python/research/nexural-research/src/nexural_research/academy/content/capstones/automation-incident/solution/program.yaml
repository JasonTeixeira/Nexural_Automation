program:
  operations:
  - load_fixture
  - expire_missing_heartbeat
  - isolate_account_namespace
  - replay_unacked_events
  - reconcile_after_disconnect
  - deduplicate_by_event_id
  - reject_late_event
  - accumulate_partial_fills
  - reject_stale_timestamp
  - seal_evidence_bundle
  settings:
    mode: paper
    deterministic: true
