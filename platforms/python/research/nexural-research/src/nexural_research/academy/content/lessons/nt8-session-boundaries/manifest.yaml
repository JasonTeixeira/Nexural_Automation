id: nt8.session-boundaries
track: nt8-foundations
title: Session Boundary Control
updated_at: '2026-07-19'
estimated_minutes: 50
objectives:
- Explain how to reset state exactly once at a trading-session boundary.
- Produce a deterministic trace and fault-evidence artifact.
prerequisites:
- nt8.multi-series
tags:
- executable
- paper-only
- duplicate
translations:
  en:
    title: Session Boundary Control
    summary: Implement and prove how to reset state exactly once at a trading-session boundary.
  es:
    title: Control de límites de sesión
    summary: Implementa y demuestra cómo reiniciar el estado una sola vez en el límite de sesión.
hints:
- Read the visible assertion, then identify how to reset state exactly once at a trading-session boundary;
  inspect `reset_on_session_boundary` as a naming clue.
- The target behavior is represented by one explicit operation near `reset_on_session_boundary`.
- Replay the duplicate profile and inspect its required recovery operation.
execution:
  runner: nexural.declarative-trace.v1
  starter: starter/program.yaml
  visible_tests: tests/visible.yaml
  hidden_tests: tests/hidden.yaml
  solution: solution/program.yaml
  expected_trace: expected-trace.json
  fault_profiles:
  - duplicate
rubric:
- id: trace-complete
  metric: tests.trace-complete.passed
  operator: equals
  expected: true
  weight: 25
  visibility: public
  message: Replace every TODO so the trusted runner can complete the trace.
- id: target-behavior
  metric: tests.target-behavior.passed
  operator: equals
  expected: true
  weight: 25
  visibility: public
  message: The trace must implement `reset_on_session_boundary`.
- id: paper-boundary
  metric: tests.paper-boundary.passed
  operator: equals
  expected: true
  weight: 25
  visibility: hidden
  message: The hidden safety boundary requires paper-only mode.
- id: fault-resilience
  metric: fault_evidence.duplicate.handled
  operator: equals
  expected: true
  weight: 25
  visibility: hidden
  message: The implementation did not safely handle the seeded duplicate scenario.
