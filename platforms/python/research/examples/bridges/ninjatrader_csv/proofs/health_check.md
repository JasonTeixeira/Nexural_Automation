# Health Check Proof

Status required before paper routing:

- Output directory exists.
- Bridge reports `ready`.
- No live account or broker credentials are required.

This example is satisfied by `CsvSignalBridge.health()`.
