# Paper Signal Roundtrip Proof

Required evidence:

- A paper `BridgeSignal` is accepted.
- The signal is written to the configured JSONL handoff file.
- The downstream consumer can parse the record without schema changes.

Live order routing remains out of scope for this example.
