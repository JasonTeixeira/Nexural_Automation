# Flatten Acknowledgement Proof

Required evidence:

- `flatten(symbol, reason)` returns an accepted acknowledgement.
- The emitted record uses `side=FLAT` and `quantity=0`.
- The reason is preserved in metadata for audit review.
