// Educational scaffold only. Validate in Strategy Analyzer and paper first.
// No same-bar execution: signals are evaluated on closed bars and entries are
// modeled for the next bar.

namespace NinjaTrader.NinjaScript.Strategies
{
    public class OpeningRangeRetest : Strategy
    {
        // Implement with Calculate.OnBarClose and explicit session guards.
        // Keep live order routing disabled until external bridge proofs pass.
    }
}

