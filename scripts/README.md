# Scripts

This folder is for repository tooling.

Examples of tooling we may add:
- module catalog generation from `metadata.yaml`
- metadata validation and formatting
- release helpers
- docs helpers

Keep scripts:
- cross-platform where possible
- non-destructive by default
- documented with usage examples
